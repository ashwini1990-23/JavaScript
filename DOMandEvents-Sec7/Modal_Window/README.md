Project : Modal Window

Goal: Building UI component- Modal Window
When user clicks on one of the buttons, a Modal window pops up and overlay which gets overlayed on the rest of the page. To close the modal window we can either click on close button, click on ovraly(outside of the window) or press Esc key on keyboard
